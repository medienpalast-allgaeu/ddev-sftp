name: sftp

pre_install_actions:

project_files:
- docker-compose.sftp.yaml
- sftp/sftp.json
- sftp/data/.gitkeep

global_files:

depencencies:

post_install_actions:

removal_actions:

yaml_read_files:
